#include <stdio.h>
#include "list.h"

/**
 * mul - multiplies an integer by another
 * @a: integer
 * @b: integer
 * Return: returns the result of the multiplication
 */

int mul(int a, int b)
{
	return (a * b);
}
