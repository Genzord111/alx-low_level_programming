#include <stdio.h>
#include "list.h"

/**
 * sub - subtracts an integer from another
 * @a: integer
 * @b: integer
 * Return: returns the result of the subtraction
 */

int sub(int a, int b)
{
	return (a - b);
}
