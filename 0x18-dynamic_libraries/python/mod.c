#include <stdio.h>
#include "list.h"

/**
 * mod - finds the modulus of two numbers
 * @a: integer
 * @b: integer
 * Return: returns the result of the modulus
 */

int mod(int a, int b)
{
	return (a % b);
}
