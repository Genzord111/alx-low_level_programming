#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: 'Code checks if number is positive, negative or zero'
 *
 * Return: Always 1 (Success)
 */

int main(void)
{
int n;
for (n = 0; n < 10; n++)
{
	putchar(n + '0');
}
putchar('\n');
return (0);
}
