#include <stdio.h>
/**
 * add - finds the addition of two numbers
 * @a: a variable containing number
 * @b: a variable containing number
 * Return: void
 */
int add(int a, int b)
{
	return (a + b);
}
